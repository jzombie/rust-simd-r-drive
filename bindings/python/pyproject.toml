[project]
name = "simd_r_drive_py"
version = "0.1.0"
description = "Python bindings for SIMD R Drive"
authors = [{ name = "Jeremy Harris", email = "jeremy.harris@zenosmosis.com" }]
license = "Apache-2.0"
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["puccinialin>=0.1.2"]
build-backend = "puccinialin"

[tool.maturin]
bindings = "pyo3"
manifest-path = "../../Cargo.toml"
